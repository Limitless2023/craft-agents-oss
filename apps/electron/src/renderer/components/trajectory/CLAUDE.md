# trajectory/

> L2 | 父级: apps/electron/src/renderer/components/CLAUDE.md

「这个会话里，模型实际收到了什么、什么时候收到、花了多久」的只读视图。入口是聊天页标题栏右侧的 Route 图标（与分享/编辑任务同一动作区）。

## 为什么它能存在而不必新建记录管线

Claude SDK 自己就在 `~/.claude/projects/<slug>/<sdkSessionId>.jsonl` 写了一份高保真轨迹：注入块逐块可见、`compactMetadata` 完整、工具原始出入参俱全、每条带时间戳、每次 API 请求带 `requestId`。craft 的 `session.jsonl` 是"对话当前样子"的快照（每轮全量重写、只有 9 种消息角色），这些一概没有。**所以这个模块只做读取与归因，不重复记录。**

唯一的例外是系统提示词——那段由 craft 在建请求时拼装，SDK 不知道，因此 `packages/shared/src/sessions/system-prompt-record.ts` 在 `claude-agent.ts` 的 options 构造点补写一份 sidecar（`<会话>/meta/system-prompt.jsonl`，内容寻址、变了才追加）。渲染层把两个源合并。

**仍读不到**：Claude Code 基座预设与工具定义在 SDK 内部。UI 里明写盲区，不假装完整。

## 成员清单

`TrajectoryOverlay.tsx`: 编排层。自持 portal（**刻意不用 `PreviewOverlay`**——那个外壳把 children 塞进带渐隐遮罩的文档滚动容器，与"顶栏固定 + 双栏各自滚 + 底部统计固定"的应用式布局冲突）。持有选中/筛选/搜索/轴口径四份状态，四个子视图共享。超长会话列表截到尾部 `LIST_CAP = 3000` 行并在顶部明示（统计与时间轴仍吃全量）。

`TrajectoryLanes.tsx`: 三泳道时间轴（Input / Model / Tools）+ 轴口径切换 + 搜索框 + **框选**。段与列表行共用 `entryIndex` 作选中键，点哪边都选中同一条。

**缩放/平移**：双指捏合或滚轮以光标为定点缩放（最高 500×），双指横扫平移，右上角显示倍数并可一键复原；换轴口径即复位（坐标含义都变了，保留缩放只会让人不知身在何处）。视口是**本地 state**——它纯粹是看的方式，不影响任何下游数据。手势必须用**原生非被动监听**：React 的 onWheel 在根节点上是被动的，拦不住默认滚动。

交互由**单独一层顶层捕获**：段本身 `pointer-events-none`，点击靠自己的几何做命中判定（渲染与命中共用同一份 `placed` 坐标，否则会出现"看得见却点不中"）。这样"点选一条"与"拖出一段"归同一个所有者，不必和按钮抢事件、也没有 z-index 打架。位移超 4px 才算拖，否则按点处理；点空白处清除框选。重叠段取**最窄**的那个——窄段被宽段罩住，不优先它就永远点不到。拖拽中的临时窗口留在本组件 state，松手才上报父级，避免每像素重渲染整个列表。

`TrajectoryList.tsx`: 密集事件流，一条目一行。框选后窗外条目**淡出而非隐藏**（保留上下文才看得出这段在整体里的位置），并自动滚到窗内第一条。工具行把调用与结果压成 `名字 {入参} → 结果`；**已配对的工具结果不单独成行**，否则同一件事列两遍。

`TrajectoryDetail.tsx`: 右栏详情，按条目类型动态给页签（工具有 入参/结果，其余只有 正文）。切条目时页签复位到概览——否则会看到上一条留下的空白页。

`TrajectorySidePanel.tsx`: 右栏的**宽度所有者**（详情与构成轮流占用同一槽位，宽度若各持一份切换时会跳）。左缘拖拽手柄，双击复位；偏好持久化（`atoms/trajectory-panel-width.ts`，localStorage）。两个坑：拖拽期间直接改 DOM 宽度**不过 React**（每像素一次 setState 会连带重渲染左侧上千行事件流，手感立刻变粘），松手才写回 atom；容器宽度必须进 state 而非渲染时读 ref（首帧 ref 是 null，读出 0 会把 clamp 掐到最小值，之后没有任何东西会触发重渲染弹回来）。窗口变窄只 clamp **显示**宽度，持久化的意图宽度不动——与 Preview 面板同一范式，窗口拉回来宽度自己回来。

`TrajectoryComposition.tsx`: 右栏**默认页**（未选中条目时）。两个维度的账：上下文构成条 + 大类筛选 chip + 注入块排行回答"**上下文预算被谁吃掉了**"，工具耗时排行回答"**时间被谁吃掉了**"，另有压缩提示与盲区声明。这块是与普通日志查看器的分水岭。工具排行**按框选窗口收窄**（框出慢的那段后要问的是"这 40 秒里谁最慢"），不受大类筛选影响——筛选是看的镜头，框选才是范围；标题上带"本段"标记，否则数字与底部统计对不上会让人以为算错了。

`kind-meta.ts`: 大类 → 角色标签/配色/泳道的唯一词典。三个视图共用，分开写必然漂移。角色标签（SYSTEM/USER/CONTEXT/TOOL…）保持英文大写常量：它是数据分类学而非文案。

解析层在 `renderer/lib/trajectory-core.ts`（+ 43 例测试），全部纯函数。

## 踩过的坑

1. **slug 规则**：斜杠**和点号**都替换为连字符（`/Users/x/.craft-agent` → `-Users-x--craft-agent`）。只替换斜杠会找不到文件。
2. **回合分组**：工具结果也是 `user` 角色，必须排除，否则每个工具结果都被当成一次新提问。
3. **token 去重**：同一 `requestId` 的每个块都带一份**完全相同**的 usage，逐块累加会把用量放大到块数倍。
4. **只调工具的请求也带 usage**：`tool_use` 条目必须挂 usage，否则"这一步只调工具不说话"的 token 凭空消失（这是最常见的形态，测试逮到过）。
5. **模型段起点回溯到上一个事件结束**：只从首个块的时间戳算，等待时间会凭空消失。也因此 UI 必须写明"时长来自会话时间戳"——含排队与网络往返，不是服务端计时。
6. **段的最小宽度加在屏幕空间，不加在轴空间**：轴空间下限会随缩放一起放大，500× 时一个零时长工具能撑满整条轨道。命中判定也必须用屏幕坐标——命中区要与看到的那根条严格一致，包括最小宽度撑出来的部分。
7. **零宽度桶取槽位正中**：`步骤`/`回合` 口径下，只含一个瞬时事件的桶若贴左缘渲染就缩成一根线，等宽分给它的那一格白分了。代价是正/反投影对这种桶不再严格互逆——瞬时事件本就不可逆，测试把这条写成了显式约定。
8. **框选跨边界的工具调用算窗内**：起点在窗前、结果落在窗内，那也是"这段时间里发生的事"，用 `endMs` 兜住。
9. **工具耗时只累加配到结果的调用**：没配对的调用没有结束时刻，按 0 计入会让慢工具显得快。`calls` 与 `timedCalls` 分开报，UI 才能在数据不全时诚实显示 `—` 而不是 `0.0s`。
10. **系统提示词一律置顶，不按时间插入**：它是会话级常量配置，从第一个 token 起就在起作用，只是我们直到某轮才抄下来；按记录时刻插会埋进对话中间，读者以为"这时候才加上的"。初版按时间插还额外踩了坑——`last-prompt` 之类的条目没有时间戳，"无时间戳即落位"的分支把它拽到了第 1 轮附近。同理它**不上时间轴泳道**：常量不是事件。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
